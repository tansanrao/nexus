# PostgreSQL Configuration
POSTGRES_PASSWORD=example
POSTGRES_PORT=5432

# API Server Configuration
API_PORT=8000
RUST_LOG=info

# Frontend Configuration
FRONTEND_PORT=80
# VITE_API_URL=/api  # Use /api for nginx proxy (default), or http://localhost:8000/api for direct connection

# Note: Create a .env file from this template for production use
# cp .env.example .env
# Then update the values accordingly
